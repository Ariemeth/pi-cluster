kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: growingsewfast

spec:
  rules:
#    - host:
    - http:
        paths:
          - path: /
            backend:
              serviceName: sw1-simpleweb
              servicePort: 9001

